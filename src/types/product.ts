export interface productType {
  productCode: number;
  productName: string;
  price: number;
  supplyPrice: number;
  vat: number;
  shippingCost: number;
  arrivalLocation: string;
  departureLocation: string;
  stock: number;
  material: string;
  origin: string;
  actions: string;
}
